#
dn: cn=schema
#
#######################
#
objectClasses: (1.3.6.1.4.1.48178.2.4.1
    NAME 'coffeeNetUser'
    DESC 'a user of the CoffeeNet (can be human or machine)'
    SUP top
    AUXILIARY
    )