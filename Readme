cd auth
mvn spring-boot:run
cd ../ui
mvn spring-boot:run

browser (aktuell bitte Chrome) öffnen
localhost:8080

login mit ldap daten
Zugriff erlauben

Neues Modul
Datei auswählen (config.yaml in ./exampleModule)

Navigationselement für "example" erscheint.
Dort hin navigieren und oberfläche anschauen.

